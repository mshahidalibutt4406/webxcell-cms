'use strict';

/**
 * generation-wealth service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generation-wealth.generation-wealth');
